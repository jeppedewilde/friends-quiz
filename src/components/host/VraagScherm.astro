<div id="vraagScherm" class="hidden">
    <!-- Het Get Ready scherm (overlay) -->
    <div id="getReadyScherm" class="hidden get-ready-scherm">
        <h1>Get Ready!</h1>
        <h2>Nieuwe vraag start in... <span id="getReadyTeller">3</span></h2>
    </div>

    <div class="vraag-header">
        <span id="vraagTeller"></span>
        <div class="timer-container">
            <div id="timerBalk"></div>
        </div>
    </div>
    
    <h3 id="vraagCategorie" class="categorie-tekst">Category: ... </h3>

    <h2 id="vraagTitel">Welke aflevering is dit?</h2>
    <img id="vraagAfbeelding" src="" alt="Aflevering">
    
    <div id="hostOpties"></div>
    <h3 id="hostUitslagTekst" class="hidden uitslag-tekst"></h3>

    <button id="eindscoreBtn" class="hidden eindscore-btn">Eindscore</button>
</div>

<style>
    #vraagScherm {
        text-align: center;
        margin-top: 2rem;
        position: relative;
    }

    .get-ready-scherm {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background-color: rgba(101, 60, 152, 0.95);
        /* 168, 118, 230 */
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        z-index: 999;
        color: white;
    }

    .get-ready-scherm h1 {
        font-size: 5rem;
        color: var(--color-yellow);
        margin-bottom: 20px;
    }

    #vraagAfbeelding {
        max-width: 100%;
        height: 25vh;
        object-fit: cover;
        border-radius: 12px;
        margin-bottom: 20px;
        transition: height 0.3s ease;
    }

    #vraagAfbeelding.grote-foto {
        height: 45vh;
        box-shadow: 0 10px 25px rgba(0,0,0,0.4);
    }

    #hostOpties {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 30px;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 40px;
        margin: 0 auto;
        box-sizing: border-box;
    }

    :global(.host-optie-knop) {
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        
        min-height: 120px; 
        padding: 20px;
        
        color: white;
        font-size: 1.8rem; 
        font-weight: bold;
        border-radius: 12px; 
        transition: all 0.3s;
        box-sizing: border-box;
        
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.4);
    }

    :global(.kleur-0) { background-color: var(--color-red); }
    :global(.kleur-1) { background-color: var(--color-blue); }
    :global(.kleur-2) { background-color: var(--color-yellow); }
    :global(.kleur-3) { background-color: var(--color-green); }
    
    :global(.fout-antwoord) { opacity: 0.3; }
    
    :global(.goed-antwoord) { 
        border: 0px solid transparent; 
        border-image-source: url(/public/images/frame.png);
        border-image-repeat: stretch;
        border-image-slice: 128;
        border-image-outset: 1.4rem;
        border-image-width: 2rem;
    }

    .uitslag-tekst {
        font-size: 2rem; 
        color: var(--secondary-color);
    }

    .eindscore-btn {
        margin-top: 30px; 
        padding: 15px 30px; 
        font-size: 1.2rem; 
        background-color: #d89e00; 
        border: none; 
        border-radius: 8px; 
        font-weight: bold; 
        cursor: pointer;
    }

    .vraag-header {
        max-width: 800px;
        margin: 0 auto 20px auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
    }

    .categorie-tekst {
        font-size: 1.2rem;
        color: var(--color-yellow);
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 10px;
        font-weight: bold;
    }

    #vraagTeller {
        font-size: 1.2rem;
        font-weight: bold;
        color: rgba(255, 255, 255, 0.8);
    }

    .timer-container {
        width: 100%;
        height: 12px;
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 6px;
        overflow: hidden;
    }

    #timerBalk {
        width: 100%;
        height: 100%;
        background-color: var(--secondary-color);
    }
    
    #vraagTitel {
        margin-bottom: 20px;
        font-family: 'Courier New', Courier, monospace;
    }

    h2 {
        margin-bottom: 10px;
    }

    #vraagCategorie, #vraagTitel {
        font-family: 'Gabriel Weiss', sans-serif;
        letter-spacing: 2px;
        font-weight: normal;
    }
</style>