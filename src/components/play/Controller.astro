<div id="gameScherm" class="hidden">
    
    <div class="antwoord-knoppen">
        <button class="antwoord-knop knop-rood" data-index="0"></button>
        <button class="antwoord-knop knop-blauw" data-index="1"></button>
        <button class="antwoord-knop knop-geel" data-index="2"></button>
        <button class="antwoord-knop knop-groen" data-index="3"></button>
    </div>

    <div id="openVraagContainer" class="hidden open-container">
        <input type="number" id="openAntwoordInput" class="name-input" placeholder="Seizoennummer...">
        <button id="stuurOpenBtn" class="join-button">Verstuur antwoord</button>
    </div>
</div>

<style>
    #gameScherm { 
        width: 100%; 
        height: 70vh; 
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        box-sizing: border-box;
        padding: 10px;
    }

    .antwoord-knoppen {
        display: grid;
        grid-template-columns: 1fr 1fr;
        grid-template-rows: 1fr 1fr;
        gap: 15px;
        width: 100%;
        height: 100%;
        box-sizing: border-box;
    }
    
    .antwoord-knop {
        border: none;
        border-radius: 12px;
        box-shadow: 4px 4px 8px rgba(0,0,0,0.2);
        width: 100%;
        height: 100%;
        outline: none;
        transition: transform 0.1s, opacity 0.1s; 
    }

    .antwoord-knop:active {
        transform: scale(0.95);
        opacity: 0.8;
    }

    .knop-rood { 
        background-color: var(--color-red, #e21b3c); 
    }
    .knop-blauw { 
        background-color: var(--color-blue, #1368ce); 
    }
    .knop-geel { 
        background-color: var(--color-yellow, #d89e00); 
    }
    .knop-groen { 
        background-color: var(--color-green, #26890c); 
    }

    .open-container {
        display: flex; 
        flex-direction: column; 
        align-items: center; 
        justify-content: center;
        height: 100%;
        width: 100%;
        gap: 15px; 
    }

    .name-input {
        padding: 15px; 
        font-size: 1.2rem; 
        width: 100%; 
        max-width: 300px; 
        text-align: center; 
        border: 2px solid #ccc; 
        border-radius: 8px;
        box-sizing: border-box;
    }

    .join-button {
        padding: 15px 30px; 
        font-size: 1.2rem; 
        font-weight: bold; 
        cursor: pointer;
        background-color: var(--secondary-color); 
        color: black; 
        border: none; 
        border-radius: 8px;
        width: 100%;
        max-width: 300px;
    }
</style>